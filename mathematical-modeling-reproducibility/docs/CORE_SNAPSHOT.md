# Core snapshot boundary

Included: `solve_all.py`, `refined_model.py`, and solver verification tests. Competition prompt, attachments, templates, source datasets, frozen outputs, paper, and AI-use record are excluded.

`solve_all.py` requires the organizer-provided input workbooks under `problem_files/附件/`; they are not included. Tests instantiate analytical and synthetic models and do not claim formal competition reproduction.

